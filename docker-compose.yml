version: '3.8'
services:
  web:
    image: nginx:stable-alpine
    container_name: playblackjack-static
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
